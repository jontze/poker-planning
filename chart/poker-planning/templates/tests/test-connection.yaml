apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "poker-planning.fullname" . }}-test-connection"
  labels:
    {{- include "poker-planning.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "poker-planning.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
